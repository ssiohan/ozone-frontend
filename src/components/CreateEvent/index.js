/* eslint-disable linebreak-style */
// == Import : npm
import React from 'react';

// == Import : local
import './createEvent.scss';

// == Composant
const CreateEvent = () => (
  <div id="createEvent">
    Banner
    Formulaire de création:
     - Titre
     - Date & durée
     - Description
     - Participants
     - Image
     - Rating: pénibilité, durée, impact société, impact environnement.
    Preview en live
  </div>
);

// == Export
export default CreateEvent;
