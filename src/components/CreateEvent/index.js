// == Import : npm
import React from 'react';

// == Import : local
import './createEvent.scss';

// == Composant
const CreateEvent = () => (
  <div id="createEvent">
    CreateEvent
  </div>
);

// == Export
export default CreateEvent;
